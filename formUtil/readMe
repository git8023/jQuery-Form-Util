Version 1.1.0
1. 表单提交
    a. 提交前回调函数, 修改提交的数据
    b. 提交后回调函数, 提交后置处理

Version 1.0.0
当前版本解决以下3类问题:
1. 表单数据获取
    a. 单值表单项, 值以字符串形式存在
    b. 多值表单项, 值以字符串数组形式存在
2. 表单项验证
    a. 正则表达式
        1). 调用验证接口时, 指定统一验证方式
        2). 定义表单项时, 指定各自的验证方式; 当前表单项中已定义时, 覆盖接口指定的正则表达式
    b. 远程验证
        1). 表单项中指定远程验证接口(URL)
        2). 定义错误消息
3. 表单回填